# User Authentication System

📌 Project Description
This project is a full-stack user authentication system featuring a Spring Boot backend and a ReactJS web application. It implements secure user registration, login, and profile management using JWT (JSON Web Tokens) or Session-based security with BCrypt password encryption.

🛠 Technologies Used
Backend:
- Java 17+
- Spring Boot 3.x (Spring Security, Spring Data JPA)
- MySQL (Database)
- BCrypt (Password Hashing)
- Maven (Dependency Management)

Frontend
- ReactJS
- Axios (API Requests)
- React Router (Navigation)
- Tailwind CSS / CSS Modules (Styling)